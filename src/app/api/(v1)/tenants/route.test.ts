// Test template for tenants root API
import { GET } from './route';
describe('GET /api/tenants', () => {
  it('should return 200 and tenants list (mock)', async () => {
    // TODO: mock prisma and test GET
  });
});
