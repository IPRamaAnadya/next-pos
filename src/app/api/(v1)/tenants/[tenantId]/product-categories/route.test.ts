// Test template for product categories API
import { GET, POST } from './route';
describe('/api/tenants/[tenantId]/product-categories', () => {
  it('should GET product categories (mock)', async () => {
    // TODO: mock prisma and test GET
  });
  it('should POST product category (mock)', async () => {
    // TODO: mock prisma and test POST
  });
});
