// Test template for notification templates API
import { GET, POST } from './route';
describe('/api/tenants/[tenantId]/notification-templates', () => {
  it('should GET notification templates (mock)', async () => {
    // TODO: mock prisma and test GET
  });
  it('should POST notification template (mock)', async () => {
    // TODO: mock prisma and test POST
  });
});
