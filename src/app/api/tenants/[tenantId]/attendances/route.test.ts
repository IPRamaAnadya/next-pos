// Test template for attendances API
import { GET, POST } from './route';
describe('/api/tenants/[tenantId]/attendances', () => {
  it('should GET attendances (mock)', async () => {
    // TODO: mock prisma and test GET
  });
  it('should POST attendance (mock)', async () => {
    // TODO: mock prisma and test POST
  });
});
