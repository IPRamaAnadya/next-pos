// Test template for today attendances API
import { GET } from './route';
describe('GET /api/tenants/[tenantId]/attendances/today', () => {
  it('should GET today attendances (mock)', async () => {
    // TODO: mock prisma and test GET
  });
});
